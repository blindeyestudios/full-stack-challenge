/**
 * An interface defining a Supervisor
 */
export interface Supervisor {
    id: number;
    phone: string;
    jurisdiction: string;
    identificationNumber: string;
    firstName: string;
    lastName: string;
}