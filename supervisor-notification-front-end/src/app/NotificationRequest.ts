/**
 * An interface defining a NotificationRequest
 */
export interface NotificationRequest {
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    supervisor: string;
}